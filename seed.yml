before_compile:
  - echo "Before compile"

before_build:
  - echo "Before build"
  - cd ./frontend && yarn

before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"
