before_compile:
  - echo "Before compile"
  - yarn

before_build:
  - echo "Before build"
  - yarn
  - cd ./frontend && yarn
  - yarn sst build --stage dev --region eu-west-2 --verbose
  - yarn sst deploy

before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"
